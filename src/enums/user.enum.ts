export enum Role {
  User = 'user',
  Owner = 'owner',
  Test = 'test',
  Admin = 'admin',
}

export enum Regist {
  Email = 'email',
  Kakao = 'kakao',
  Naver = 'naver',
  Apple = 'apple',
  Google = 'google',
}
